"""Получение и обновление OAuth-токенов Яндекса — см. yandex_oauth.cli.

Инвариант: значения секретов никогда не печатаются в stdout/stderr —
только отпечаток sha256:xxxxxxxx. Всё хранится в Keychain, на диске
в открытом виде ничего не остаётся.
"""

__version__ = "1.1.0"
