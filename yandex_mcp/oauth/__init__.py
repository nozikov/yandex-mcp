"""OAuth-часть: PKCE-вход в Яндекс и приём redirect на localhost.

Команды пользователя живут в `yandex_mcp.cli` (`yandex-mcp login|setup`),
здесь — сам флоу.

Инвариант: значения секретов никогда не печатаются в stdout/stderr — только
отпечаток sha256:xxxxxxxx. Где именно хранится токен, решает
`yandex_mcp.secrets`: системное хранилище ОС, а если его нет — файл 0600.
"""
